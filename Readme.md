# Colorization of BW images to RGB

## Requirements

Project requires 
 - tensorflow with CUDA support(can be modified for only CPU usage)
 - Python 2.7
 - Dataset of images

##Usage

`python bw_rgb2.py`